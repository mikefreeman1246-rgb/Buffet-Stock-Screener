services:
  backend:
    build: ./backend
    ports:
      - "9010:9000"
    volumes:
      - ./data:/data
    environment:
      - PYTHONUNBUFFERED=1
      - CORS_ORIGINS=http://localhost:3010

  frontend:
    build: ./frontend
    ports:
      - "3010:80"
    depends_on:
      - backend
