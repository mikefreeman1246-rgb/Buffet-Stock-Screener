services:
  backend:
    build: ./backend
    ports:
      - "9000:9000"
    volumes:
      - ./data:/data
    environment:
      - PYTHONUNBUFFERED=1
      - CORS_ORIGINS=http://localhost:3002

  frontend:
    build: ./frontend
    ports:
      - "3002:80"
    depends_on:
      - backend
