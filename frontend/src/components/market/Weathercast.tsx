/**
 * Market Weathercast dashboard — TEMPORARY placeholder.
 * The real hero + indicator cards + settings drawer arrive in the next phase
 * (plan tasks 11-14). This keeps the app compiling once the weather tab is wired.
 */
export default function Weathercast() {
  return <div>coming soon</div>;
}
